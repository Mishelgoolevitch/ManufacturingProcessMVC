@using ManufacturingProcessMVC.ViewModels
@model IEnumerable<ManufacturingProcess>
<span style="color: hsl(var(--primary)); font-weight: 500;"></span>
<span style="color: hsl(var(--primary)); font-weight: 500;"></span>

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>
            <i class="bi-list-task text-primary"></i> Manufacturing Processes
        </h1>
        <a asp-action="Create" class="btn btn-primary">
            <i class="bi-plus-circle"></i> New Process
        </a>
    </div>
    
    @* <div class="row">
        <span style="color: hsl(var(--primary)); font-weight: 500;"></span> *@
    @*         @foreach (var process in Model)
        {
            <div class="col-md-6 col-lg-4 mb-4">
                <div class="card process-card h-100">
                    <!-- Исправлено: правильный синтаксис для условного класса -->
                    <div class="card-header [(process.Status](workspace://(process.Status) == "Completed" ? "bg-success" : 
                                              process.Status == "InProgress" ? "bg-warning" : "bg-secondary") text-white">
                        <h5 class="card-title mb-0">
                            [process.Name](workspace://process.Name)
                        </h5>
                    </div>
                    
                    <div class="card-body">
                        <div class="row mb-3">
                            <div class="col-6">
                                <strong>Drill:</strong><br>
                                <span class="badge bg-info">
                                    [(process.Drill](workspace://(process.Drill)?.Diameter) mm
                                </span>
                            </div>
                            <div class="col-6">
                                <strong>Tap:</strong><br>
                                <span class="badge bg-info">
                                    [process.Tap](workspace://process.Tap)?.Size
                                </span>
                            </div>
                        </div>
                        
                        <div class="mb-3">
                            <strong>Status:</strong>
                            <!-- Исправлено: правильный синтаксис для условного класса -->
                            <span class="badge [(process.Status](workspace://(process.Status) == "Completed" ? "bg-success" : 
                                                 process.Status == "InProgress" ? "bg-warning" : "bg-secondary")">
                                [process.Status](workspace://process.Status)
                            </span>
                        </div>
                        
                        <div class="mb-3">
                            <strong>Created:</strong><br>
                            [process.CreatedDate.ToString](workspace://process.CreatedDate.ToString)("dd.MM.yyyy HH:mm")
                        </div>
                    </div>
                    
                    <div class="card-footer">
                        <div class="d-flex justify-content-between">
                            <a asp-action="Details" asp-route-id="[process.Id](workspace://process.Id)"
                               class="btn btn-sm btn-outline-primary">
                                <i class="bi-eye"></i> Details
                            </a>
                            
                    <span style="color: hsl(var(--primary)); font-weight: 500;"></span>
                    @if(process.Status != "Completed")
                            {
                                <form asp-action="Execute" asp-route-id="[process.Id](workspace://process.Id)" method="post">
                                    <button type="submit" class="btn btn-sm btn-success">
                                        <i class="bi-play-circle"></i> Execute
                                    </button>
                                </form>
                            }
                        </div>
                    </div>
                </div>
            </div>
        }
    </div> *@
</div>
